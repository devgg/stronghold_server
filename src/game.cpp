//
//  game.cpp
//  stronghold_server
//
//  Created by Mikael Beyene on 18/08/15.
//
//

#include "game.hpp"
#include <iostream>

namespace game {
    /*
    auto game::key_event(constants::key key, constants::event event, communication::client c) -> void {
        proxy->key_event(key, event, c);
    }
    
    
    auto game::client_connected(std::string user_name, communication::client c) -> void {
        players[c] = {user_name, c};
        
//        std::cout << "Player " << user_name << " connected" << std::endl;
    }
    
    auto game::client_disconnected(communication::client c) -> void {
        player p = players[c];
        players.erase(c);
        
//        std::cout << "Player " << p.get_username() << " disconnected" << std::endl;
    }*/
}